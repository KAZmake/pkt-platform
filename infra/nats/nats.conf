# NATS JetStream конфигурация для PKT Platform
# Задача 1.5

port: 4222
http_port: 8222

# JetStream — включаем с хранилищем на диске
jetstream {
  store_dir: "/data"
  max_memory_store: 512MB
  max_file_store: 2GB
}

# Логирование
logtime: true
debug: false
trace: false
